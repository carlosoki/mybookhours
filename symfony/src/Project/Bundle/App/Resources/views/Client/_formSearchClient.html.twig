{#<form action="{{ action }}" method="{{ method }}" id="{{ form.vars.id }}" class="form-horizontal">#}
    <div class="form-body">
        <div class="form-group">
            <label class="col-md-3 control-label">By name:</label>
            <div class="col-md-6">
                <div class="input-icon">
                    <input type="text" class="names form-control" value="" data-role="tagsinput">
                    <span class="help-block"> Names separated by "," or "Enter" </span>
                </div>
            </div>
        </div>
    </div>
    <div class="form-actions">
        <div class="row">
            <div class="col-md-offset-3 col-md-9">
                <button type="button"
                        action="{{ action }}" editUrl="{{ path('app_client_edit', {'id': 'client_id'}) }}"
                        deleteUrl="{{ path('api_client_delete', { 'id': 'client_id'}) }}"
                        class="search btn btn-circle blue">
                            <i class="icon-magnifier"></i>
                                Search
                </button>
            </div>
        </div>
    </div>
{#</form>#}
